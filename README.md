# OpenGL-Triangle
🔺 OpenGL Triangle  A simple OpenGL 3.3 Core project that renders a colorful 3D-positioned triangle using GLFW, GLAD, and GLSL shaders.  This project is based on the fundamentals of the OpenGL rendering pipeline — creating a window, sending vertex data to the GPU, compiling shaders, and drawing geometry.
